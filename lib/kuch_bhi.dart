import 'package:flutter/material.dart';

class KuchBhi extends StatelessWidget {
  const KuchBhi({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Kuch bhi"),
    );
  }
}
